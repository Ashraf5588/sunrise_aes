<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Enter Attendance</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #f0f8ff 0%, #e6f3ff 100%);
      min-height: 100vh;
      padding: 20px;
      color: #2c3e50;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      background: white;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .header {
      background: linear-gradient(135deg, #87ceeb 0%, #b0e0e6 100%);
      color: #2c3e50;
      text-align: center;
      padding: 30px;
      margin-bottom: 0;
    }

    h1 {
      font-size: 28px;
      font-weight: 600;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin: 0;
    }

    .form-container {
      padding: 30px;
    }

    .table-wrapper {
      overflow-x: auto;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
      margin-bottom: 30px;
      width: 100%;
    }

    table {
      width: 100%;
      min-width: 1000px; /* Minimum width to prevent cramping */
      border-collapse: collapse;
      background: white;
      font-size: 14px;
      table-layout: fixed; /* Fixed layout for better control */
    }

    th {
      background: linear-gradient(135deg, #87ceeb 0%, #b0e0e6 100%);
      color: #2c3e50;
      padding: 16px 12px;
      text-align: center;
      font-weight: 600;
      font-size: 14px;
      border: none;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      white-space: nowrap;
    }

    /* Define column widths for better spacing */
    th:nth-child(1), td:nth-child(1) { width: 10%; } /* Reg No */
    th:nth-child(2), td:nth-child(2) { width: 8%; }  /* Roll */
    th:nth-child(3), td:nth-child(3) { width: 20%; } /* Name */
    th:nth-child(4), td:nth-child(4) { width: 8%; }  /* Class */
    th:nth-child(5), td:nth-child(5) { width: 8%; }  /* Section */
    th:nth-child(6), td:nth-child(6) { width: 11.5%; } /* First Term */
    th:nth-child(7), td:nth-child(7) { width: 11.5%; } /* Second Term */
    th:nth-child(8), td:nth-child(8) { width: 11.5%; } /* Third Term */
    th:nth-child(9), td:nth-child(9) { width: 11.5%; } /* Final Term */

    td {
      padding: 12px 8px;
      text-align: center;
      border-bottom: 1px solid #f0f8ff;
      vertical-align: middle;
    }

    tbody tr:nth-child(even) {
      background-color: #fafcff;
    }

    tbody tr:nth-child(odd) {
      background-color: #ffffff;
    }

    tbody tr:hover {
      background-color: #f0f8ff;
      transform: translateY(-1px);
      box-shadow: 0 2px 8px rgba(135, 206, 235, 0.15);
      transition: all 0.3s ease;
    }

    input[type="text"], input[type="number"] {
      width: 100%;
      min-width: 0; /* Allow shrinking */
      padding: 8px 6px;
      border: 2px solid #e3f2fd;
      border-radius: 6px;
      font-size: 14px;
      transition: all 0.3s ease;
      background: white;
      color: #2c3e50;
      box-sizing: border-box;
    }

    input[type="text"]:focus, input[type="number"]:focus {
      outline: none;
      border-color: #87ceeb;
      box-shadow: 0 0 0 3px rgba(135, 206, 235, 0.2);
      background: #f8fdff;
    }

    input[type="text"]:read-only {
      background: #f8f9fa;
      color: #6c757d;
      font-size: 13px;
    }

    /* Specific styling for number inputs */
    input[type="number"] {
      text-align: center;
      font-weight: 600;
    }

    .submit-btn {
      background: linear-gradient(135deg, #4fc3f7 0%, #29b6f6 100%);
      color: white;
      border: none;
      padding: 15px 40px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      box-shadow: 0 4px 15px rgba(41, 182, 246, 0.3);
      display: block;
      margin: 0 auto;
    }

    .submit-btn:hover {
      background: linear-gradient(135deg, #29b6f6 0%, #0288d1 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(41, 182, 246, 0.4);
    }

    .submit-btn:active {
      transform: translateY(0);
      box-shadow: 0 2px 10px rgba(41, 182, 246, 0.3);
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      body {
        padding: 10px;
      }
      
      .container {
        border-radius: 12px;
      }
      
      .header {
        padding: 20px;
      }
      
      h1 {
        font-size: 22px;
      }
      
      .form-container {
        padding: 15px;
      }
      
      .table-wrapper {
        margin-bottom: 20px;
        /* Enable horizontal scrolling on mobile */
        -webkit-overflow-scrolling: touch;
      }
      
      table {
        min-width: 800px; /* Reduced minimum width for mobile */
        font-size: 12px;
      }
      
      th, td {
        padding: 8px 4px;
        font-size: 12px;
      }
      
      th {
        font-size: 11px;
        padding: 12px 4px;
      }
      
      input[type="text"], input[type="number"] {
        padding: 6px 4px;
        font-size: 12px;
        border-radius: 4px;
      }
      
      input[type="text"]:read-only {
        font-size: 11px;
      }
      
      /* Adjust column widths for mobile */
      th:nth-child(1), td:nth-child(1) { width: 12%; } /* Reg No */
      th:nth-child(2), td:nth-child(2) { width: 8%; }  /* Roll */
      th:nth-child(3), td:nth-child(3) { width: 25%; } /* Name */
      th:nth-child(4), td:nth-child(4) { width: 8%; }  /* Class */
      th:nth-child(5), td:nth-child(5) { width: 8%; }  /* Section */
      th:nth-child(6), td:nth-child(6) { width: 9.75%; } /* First Term */
      th:nth-child(7), td:nth-child(7) { width: 9.75%; } /* Second Term */
      th:nth-child(8), td:nth-child(8) { width: 9.75%; } /* Third Term */
      th:nth-child(9), td:nth-child(9) { width: 9.75%; } /* Final Term */
    }

    @media (max-width: 480px) {
      .header {
        padding: 15px;
      }
      
      h1 {
        font-size: 18px;
      }
      
      .form-container {
        padding: 10px;
      }
      
      table {
        min-width: 700px;
        font-size: 11px;
      }
      
      th, td {
        padding: 6px 3px;
        font-size: 11px;
      }
      
      th {
        font-size: 10px;
        padding: 10px 3px;
      }
      
      input[type="text"], input[type="number"] {
        padding: 5px 3px;
        font-size: 11px;
      }
      
      input[type="text"]:read-only {
        font-size: 10px;
      }
      
      .submit-btn {
        padding: 12px 30px;
        font-size: 14px;
      }
    }

    /* Loading animation */
    .loading {
      opacity: 0.7;
      pointer-events: none;
    }

    .loading::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 40px;
      height: 40px;
      border: 4px solid #f3f3f3;
      border-top: 4px solid #87ceeb;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      transform: translate(-50%, -50%);
    }

    @keyframes spin {
      0% { transform: translate(-50%, -50%) rotate(0deg); }
      100% { transform: translate(-50%, -50%) rotate(360deg); }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Attendance Entry</h1>
      <p style="margin-top: 10px; font-size: 16px; opacity: 0.9;">Class <%= studentClass %> - Section <%= section %> - Subject <%= subject %></p>
    </div>
    
    <div class="form-container">
      <form action="/attendance?studentClass=<%=studentClass%>&section=<%=section%>&terminal=<%=terminal%>&academicYear=<%=academicYear%>&subject=<%=subject%>" method="POST">
        <div class="table-wrapper">
          <table>
            <thead>
              <tr>
                <th>Reg No.</th>
                <th>Roll</th>
                <th>Name</th>
                <th>Class</th>
                <th>Section</th>
                <th>First Term</th>
                <th>Second Term</th>
                <th>Third Term</th>
                <th>Final Term</th>
              </tr>
            </thead>
            <tbody>
              <% studentData.forEach((student,index) => { %>
                <tr>
                  <td><input type="text" name="students[<%= index %>][reg]" value="<%= student.reg %>" readonly></td>
                  <td><input type="text" name="students[<%= index %>][roll]" value="<%= student.roll %>" readonly></td>
                  <td><input type="text" name="students[<%= index %>][name]" value="<%= student.name %>" readonly></td>
                  <td><input type="text" name="students[<%= index %>][class]" value="<%= student.studentClass %>" readonly></td>
                  <td><input type="text" name="students[<%= index %>][section]" value="<%= student.section %>" readonly></td>
                  <td><input type="number" name="students[<%= index %>][firstTerm]" min="0" max="100" value="<%= attendanceData[index]?.firstTerm || '' %>" ></td>
                  <td><input type="number" name="students[<%= index %>][secondTerm]" min="0" max="100" value="<%= attendanceData[index]?.secondTerm || '' %>" ></td>
                  <td><input type="number" name="students[<%= index %>][thirdTerm]" min="0" max="100" value="<%= attendanceData[index]?.thirdTerm || '' %>" ></td>
                  <td><input type="number" name="students[<%= index %>][finalTerm]" min="0" max="100" value="<%= attendanceData[index]?.finalTerm || '' %>" ></td>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>
        <button type="submit" class="submit-btn">Submit Attendance</button>
      </form>
    </div>
  </div>
</body>
</html>